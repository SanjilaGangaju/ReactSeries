export const TodoItemsWrapper = ()=>{
    return (
         <ul>
           {task.map((currTask)=>{
           const {index, checked, todo}  = currTask;
           })};
           </ul>
        
    )
}